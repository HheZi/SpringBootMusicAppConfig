spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/TrackDB
    password: postgres
    username: postgres
  kafka:
    bootstrap-servers:
    - localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "com.app.kafka.message"
server:
  port: 0
file.temp: /Programming/Java/SpringBootMusicApp/Backend/track-service/temp
file.max-size: 37748736
kafka.topic.name: track-deletion-topic
